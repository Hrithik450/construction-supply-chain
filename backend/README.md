# Backend

FastAPI backend for the Autonomous Construction Supply Chain Brain.

## Run

```bash
uv sync
uv run uvicorn app.main:app --reload
```

## Endpoints

- `GET /health`
- `GET /api/v1/overview`
